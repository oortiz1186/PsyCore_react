'use client';
import { FormEvent,useEffect,useState } from 'react';
import { getSupabaseBrowser } from '@/lib/supabase-browser';
type Patient={id:string;first_name?:string;last_name?:string;full_name?:string;email?:string;phone?:string;birth_date?:string};
export default function Patients(){const [rows,setRows]=useState<Patient[]>([]);const [show,setShow]=useState(false);const [form,setForm]=useState({first_name:'',last_name:'',email:'',phone:'',birth_date:''});const [msg,setMsg]=useState('');async function load(){const {data,error}=await getSupabaseBrowser().from('patients').select('*').order('created_at',{ascending:false});if(error)setMsg(error.message);else setRows((data||[]) as Patient[]);}useEffect(()=>{void load()},[]);async function save(e:FormEvent){e.preventDefault();setMsg('');const s=getSupabaseBrowser();const {data:{user}}=await s.auth.getUser();const payload={...form,birth_date:form.birth_date||null,created_by:user?.id};const {error}=await s.from('patients').insert(payload);if(error){setMsg(error.message);return;}setForm({first_name:'',last_name:'',email:'',phone:'',birth_date:''});setShow(false);await load();}return <><div className="page-head"><div><h1>Pacientes</h1><p className="muted">Expedientes y datos generales</p></div><button className="btn btn-primary" onClick={()=>setShow(!show)}>{show?'Cancelar':'Nuevo paciente'}</button></div>{show&&<form className="card form" onSubmit={save}><div className="two"><label className="field">Nombre<input value={form.first_name} onChange={e=>setForm({...form,first_name:e.target.value})} required/></label><label className="field">Apellidos<input value={form.last_name} onChange={e=>setForm({...form,last_name:e.target.value})} required/></label></div><div className="two"><label className="field">Correo<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label className="field">Teléfono<input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label></div><label className="field">Fecha de nacimiento<input type="date" value={form.birth_date} onChange={e=>setForm({...form,birth_date:e.target.value})}/></label><button className="btn btn-primary">Guardar paciente</button></form>}{msg&&<div className="error">{msg}</div>}<div className="card table-wrap"><table className="table"><thead><tr><th>Nombre</th><th>Correo</th><th>Teléfono</th><th>Nacimiento</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.full_name||`${r.first_name||''} ${r.last_name||''}`}</td><td>{r.email||'—'}</td><td>{r.phone||'—'}</td><td>{r.birth_date||'—'}</td></tr>)}</tbody></table></div></>}

'use client';
import { FormEvent,useState } from 'react';
import { getSupabaseBrowser } from '@/lib/supabase-browser';
export default function SmtpSettings(){const [email,setEmail]=useState('');const [msg,setMsg]=useState('');const [ok,setOk]=useState(false);async function test(e:FormEvent){e.preventDefault();setMsg('');setOk(false);const {data:{session}}=await getSupabaseBrowser().auth.getSession();const res=await fetch('/api/smtp/test',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${session?.access_token||''}`},body:JSON.stringify({to:email})});const data=await res.json();if(!res.ok){setMsg(data.error||'No se pudo enviar la prueba.');return;}setOk(true);setMsg('Correo de prueba enviado correctamente.');}return <><div className="page-head"><div><h1>Configuración SMTP</h1><p className="muted">Las credenciales se protegen en variables de entorno del servidor.</p></div></div><section className="card"><h2>Variables requeridas</h2><p className="muted">Configura SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASSWORD, SMTP_FROM_EMAIL y SMTP_FROM_NAME en <code>.env.local</code>.</p><form className="form" onSubmit={test}><label className="field">Correo para prueba<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><button className="btn btn-primary">Enviar prueba</button></form>{msg&&<div className={ok?'success':'error'}>{msg}</div>}</section></>}

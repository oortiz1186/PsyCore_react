import { NextRequest,NextResponse } from 'next/server';
import nodemailer from 'nodemailer';
import { getSupabaseAdmin } from '@/lib/supabase-admin';
async function adminOnly(req:NextRequest){const token=req.headers.get('authorization')?.replace('Bearer ','');if(!token)return false;const a=getSupabaseAdmin();const {data}=await a.auth.getUser(token);if(!data.user)return false;const {data:p}=await a.from('profiles').select('roles(name)').eq('id',data.user.id).maybeSingle();const role=Array.isArray(p?.roles)?p.roles[0]?.name:(p?.roles as {name?:string}|null)?.name;return role==='Administrador';}
export async function POST(req:NextRequest){if(!await adminOnly(req))return NextResponse.json({error:'No autorizado.'},{status:403});const {to}=await req.json();const host=process.env.SMTP_HOST,user=process.env.SMTP_USER,pass=process.env.SMTP_PASSWORD,from=process.env.SMTP_FROM_EMAIL;if(!host||!user||!pass||!from)return NextResponse.json({error:'Falta configurar SMTP en .env.local.'},{status:400});try{const t=nodemailer.createTransport({host,port:Number(process.env.SMTP_PORT||587),secure:Number(process.env.SMTP_PORT)===465,auth:{user,pass}});await t.sendMail({from:`${process.env.SMTP_FROM_NAME||'PsyCore'} <${from}>`,to,subject:'Prueba SMTP de PsyCore',html:'<h2>Configuración correcta</h2><p>PsyCore puede enviar correos.</p>'});return NextResponse.json({ok:true});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'No se pudo enviar.'},{status:500});}}
